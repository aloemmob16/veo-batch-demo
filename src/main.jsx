import React from "react";
import ReactDOM from "react-dom/client";
import VeoBatchApp from "./VeoBatchApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VeoBatchApp />
  </React.StrictMode>
);
